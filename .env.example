# Optional: default AI provider and key when user leaves "API Key" empty in AI Settings.
# For local dev: copy this file to .env.local and add your key (do not commit .env.local).
# For Amplify: set these in App settings → Environment variables.

# VITE_AI_DEFAULT_PROVIDER=groq
# VITE_AI_DEFAULT_API_KEY=your_key_here
